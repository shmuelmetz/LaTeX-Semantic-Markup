% Shared document body used by test-style-default.tex and
% test-style-plain.tex to verify the journal-portability property:
% this file is byte-for-byte identical under every style: only the
% \usepackage[style=...] line in the wrapper files differs.
Category name: $\catName{A}$, long name: $\catName{Set}$.

Sequence of categories: $\catSeqName{A}$.

Object name: $\objName{x}$.

Morphism: $\morphName{f} \colon \objName{x} \morphMono \objName{y}$,
and $\objName{x} \morphEpi \objName{y}$.

Sequence of morphisms: $\morphSeqName{f}$.

Composition: $\morphName{g} \morphCompose \morphName{f}$,
$\morphName{g} \morphComposeHead \morphName{f}$,
$\morphName{g} \morphComposeTail \morphName{f}$.

Chart/atlas/coordinate: $\chartName{U}$, $\atlasName{\mathcal{A}}$,
$\coordName{x}$.

Generic: \semName{plain text}.
